<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_concluir_venda"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFDF8"
    tools:context=".ConcluirVendaActivity">

    <!-- Título superior -->
    <TextView
        android:id="@+id/txtConcluir"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Concluir venda"
        android:textColor="#888888"
        android:textSize="20sp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Imagem do livro -->
    <ImageView
        android:id="@+id/imgLivro"
        android:layout_width="200dp"
        android:layout_height="280dp"
        android:layout_marginStart="48dp"
        android:scaleType="fitCenter"
        android:src="@drawable/exemplo_capa"
        app:layout_constraintTop_toBottomOf="@id/txtConcluir"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Informações -->

    <!-- Botões -->
    <TextView
        android:id="@+id/txtInfoLivro"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="48dp"
        android:background="#E6F7F7"
        android:padding="16dp"
        android:text="Título do livro, Autor\nPreço\nOutras informações"
        android:textColor="#333333"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="@id/imgLivro"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.967"
        app:layout_constraintStart_toEndOf="@id/imgLivro"
        app:layout_constraintTop_toTopOf="@id/imgLivro"
        app:layout_constraintVertical_bias="0.48" />

    <Button
        android:id="@+id/btnCancelarVenda"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="CANCELAR VENDA"
        android:backgroundTint="#009999"
        android:textColor="#FFFFFF"
        android:textAllCaps="true"
        android:textStyle="bold"
        android:layout_marginHorizontal="48dp"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@id/imgLivro"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btnConfirmarVenda"
        app:layout_constraintHorizontal_chainStyle="spread" />

    <Button
        android:id="@+id/btnConfirmarVenda"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="48dp"
        android:layout_marginTop="32dp"
        android:backgroundTint="#009999"
        android:text="CONFIRMAR VENDA"
        android:textAllCaps="true"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/btnCancelarVenda"
        app:layout_constraintTop_toBottomOf="@id/imgLivro" />

</androidx.constraintlayout.widget.ConstraintLayout>
